# DataMind AI - backend dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-multipart==0.0.12

# Auth / JWT
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 is incompatible with bcrypt>=4.1 (backend detection raises on
# >72-byte probe). Pin to the last 4.0.x release.
bcrypt==4.0.1

# Database
sqlalchemy==2.0.35
alembic==1.13.3
# Postgres driver (optional at runtime; SQLite works out of the box)
psycopg[binary]==3.2.3

# Data engine
pandas==2.2.3
numpy==2.1.2
scikit-learn==1.5.2
duckdb==1.1.1
openpyxl==3.1.5

# Visualization engine (hybrid: Plotly specs client-side; Matplotlib/Seaborn
# render statistical/publication charts server-side to base64 PNG + SVG).
matplotlib==3.11.1
seaborn==0.13.2
kaleido==1.3.0

# Model Studio (Phase 3): hyperparameter tuning + gradient boosting + SHAP
# explainability. All optional at runtime - the ML engine detects each library
# and falls back to scikit-learn (RandomizedSearchCV / permutation importance).
optuna==4.9.0
xgboost==3.3.0
lightgbm==4.7.0
shap==0.52.0

# Storage (S3/MinIO) - optional
boto3==1.35.36

# Professional report exports (PDF / Word / PowerPoint). Markdown & HTML are
# rendered natively. All optional at runtime - the export endpoint reports
# which formats are available.
reportlab==4.2.5
python-docx==1.1.2
python-pptx==1.0.2

# AI (optional; only needed when AI_PROVIDER=openai)
openai==1.51.0

# Testing
pytest==8.3.3
httpx==0.27.2
